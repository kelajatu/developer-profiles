export const centered = (color) => {
    return`
        display: flex;
        flex-flow: column;
        align-items: center;
        justify-content: center;
        width: 100%;
        background: ${color}
`}